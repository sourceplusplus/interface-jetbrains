sudo: required

language: java
jdk: oraclejdk11

services:
  - docker

env:
  global:
    - JAVA_TOOL_OPTIONS=-Dhttps.protocols=TLSv1.2

before_install:
 - chmod +x gradlew
 - curl -v -H "Accept: application/vnd.github.everest-preview+json" -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/sourceplusplus/assistant/dispatches -d '{"event_type":"test-event"}'

before_cache:
  - rm -rf $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
    - $HOME/.gradle/caches/modules-2
    - $HOME/.gradle/wrapper/

deploy:
  provider: releases
  api_key: $GH_TOKEN
  file:
  - /home/travis/build/sourceplusplus/Assistant/agent/jvm-agent/build/libs/source-agent-0.2.5.jar
  - /home/travis/build/sourceplusplus/Assistant/api/build/libs/source-api-0.2.5.jar
  - /home/travis/build/sourceplusplus/Assistant/core/build/distributions/source-core-0.2.5.zip
  - /home/travis/build/sourceplusplus/Assistant/plugin/jetbrains-plugin/build/distributions/jetbrains-source-plugin-0.2.5.zip
  - /home/travis/build/sourceplusplus/Assistant/portal/build/distributions/source-portal-0.2.5.zip
  skip_cleanup: true
  on:
    tags: true

# after_deploy: curl -v -H "Accept: application/vnd.github.everest-preview+json" -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/sourceplusplus/assistant/dispatches -d '{"event_type":"test-event"}'
