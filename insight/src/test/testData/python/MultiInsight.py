import random


def probabilityAndDuration():
    if random.random() > 0.5:  # sleep 100ms
        if random.random() > 0.5:  # sleep 100ms
            Thread.sleep(200)

    else:
        Thread.sleep(200)
